#include <bits/stdc++.h>  //Includes all the files

#include <unordered_map>
typedef long long ll;
typedef unsigned long long ull;

#define MOD 1000000007

#define IOS                           \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL);
/*Used to desyncronize cin and cout with printf and scanf*/

using namespace std;

int main() {
    //Time O(n)
    int n = 10;
    for (int i = 0; i < n; i++) {
    }
    //Time Complexity O(logn)
    for (int i = 0; i < n; i++) {
        n = n / 2;
    }
    //Time Complexity O(n^3)
    for(int i = 0;i<n;i++){
        for(int i = 0;i<n;i++){
            for(int i = 0;i < n;i++){
            
            }
        }
    }

}
